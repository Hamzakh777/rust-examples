fn main() {
    // this is a macro because it has ! in the end
    // if it were a function it would be "println"
    println!("Hello world!");
}

// Compiling and running the code are separate 
// rustc compiles and then outputs a binary executable 